name: CI Pipeline
on:
  push:
    branches:
      - master 
      - feature
jobs:
  build:
    runs-on: self-hosted
      
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

    