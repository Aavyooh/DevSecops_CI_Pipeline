name: CI Pipeline
on:
  push:
    branches:
      - master 
      - feature
jobs:
  build:
    runs-on: 
      - ubuntu-runner
      - self-hosted
      - prod
      - dev
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

    